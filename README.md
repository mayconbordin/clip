clip
====

A simple command line interface for PHP.


### Usage ###

Start the library:

```php
require '../src/clip.php';
$clip = new Clip();
```

And create your commands:

```php
require '../src/clip.php';
$clip = new Clip();
```
